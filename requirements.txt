# Pinned for reproducibility (see REPRODUCTION.md for the exact tested versions).
#
# claude-agent-sdk drives the baseline and the agent. It authenticates the
# same way the Claude Code CLI does (checks ANTHROPIC_API_KEY first, falls
# back to a logged-in Claude subscription session) -- no separate API key
# is required if you already run Claude Code under a Pro/Max plan. See
# CHANGELOG.md for why this replaced a raw-Anthropic-SDK design.
claude-agent-sdk==0.2.147
pytest==9.1.1
pytest-randomly==4.1.0
pyyaml==6.0.3

# Only used by the optional local web UI (flakedoctor/web, run with
# `python -m flakedoctor.web`). Everything else in this project runs from
# the CLI alone; see flakedoctor/web/__init__.py for what this is and,
# just as importantly, is not (a hosted, multi-user product).
fastapi==0.136.3
uvicorn==0.49.0
