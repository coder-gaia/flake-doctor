[pytest]
# Scope bare `pytest` invocations to the project's own harness tests.
# benchmark/cases/*/tests are flaky by design and must only be run
# explicitly, one case at a time, through flakedoctor.detect / .verify --
# never swept up by an unqualified `pytest` from the repo root.
testpaths = tests
